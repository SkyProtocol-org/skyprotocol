{-# OPTIONS_GHC -Wno-orphans #-}

-- NOTE: This module should only be imported separately wherever it is needed!!!
-- This module contains neccessary instances for the SkyDA types for the onchain part of the app
module Common.OnChain where

-- import Common
-- import qualified PlutusTx as P
-- import qualified PlutusTx.Blueprint as P
-- import qualified PlutusTx.Builtins as P
-- import qualified PlutusTx.Prelude as P
-- import qualified PlutusTx.Show as P

-- deriving newtype instance P.Show (FixedLengthByteString len)
-- deriving via P.BuiltinByteString instance P.Eq (FixedLengthByteString len)
-- deriving via P.BuiltinByteString instance P.ToData (FixedLengthByteString len)
-- deriving via P.BuiltinByteString instance ToInt (FixedLengthByteString len)
-- deriving anyclass instance P.HasBlueprintDefinition (FixedLengthByteString len)
